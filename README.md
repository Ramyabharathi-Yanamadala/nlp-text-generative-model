# nlp-text-generative-model
Generative Text Model using GPT/LSTM. This project builds a text generation system capable of producing coherent paragraphs based on user prompts, with preprocessing, model training, and generated text examples.
